{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Formulário</h1>
    {{ form(form) }}

    <a href="{{ path('post') }}" >
            Voltar para a lista de posts
        </a>

{% endblock %}
